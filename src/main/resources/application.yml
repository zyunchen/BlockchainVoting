

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invoice_management?serverTimezone=UTC
    username: root
    password: Coen275!
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
  main:
    allow-circular-references: true

  #    jndi-name: java:jboss/datasources/customers


server:
  port: 8888

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
